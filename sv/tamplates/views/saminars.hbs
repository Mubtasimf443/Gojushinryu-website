<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Seminars | GojushinRyu</title>
  {{>Links}}
  <link href="https://fonts.googleapis.com/css2?family=Libre+Franklin:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  
  <style>/* Global Styles */
:root {
    --bg-color: whitesmoke;
    --card-bg: #ffffff;
    --accent-color: #ffaa1c;
    --text-color: #333;
    --font-primary: 'Libre Franklin', sans-serif;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: var(--font-primary);
    background-color: var(--bg-color);
    color: var(--text-color);
}

main {
    padding-top: 40px;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

.page-header {
    text-align: center;
    margin-bottom: 40px;
}

.page-header h1 {
    font-size: 2.5rem;
    color: var(--accent-color);
}

.page-header p {
    font-size: 1.2rem;
}

/* Responsive Header */
@media screen and (max-width: 575px) {
    .page-header h1 {
        font-size: 24px;
    }
    .page-header p {
        font-size: 17.5px;
    }
}

/* Seminars Grid */
.seminars-grid {
    display: flex;
    grid-template-columns:row;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;
    row-gap: 30px;
    gap: 30px;
    
    padding: 20px;
}

/* Seminar Card */
.seminar-card {
    background: var(--card-bg);
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    max-width: 500px;
    min-height: 500px;
}

.seminar-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
}

.seminar-card img {
    width: 100%;
    min-height: 200px;
    height: fit-content;
    max-height:400px;
    object-fit: cover;
}

.seminar-info {
    padding: 15px;
    text-align: left;
}

.seminar-info h3 {
    font-size: 1.3rem;
    margin-bottom: 8px;
    color: var(--accent-color);
}

.seminar-meta {
    font-size: 0.95rem;
    font-weight: 600;
    color: #555;
    margin-top: 8px;
}

.seminar-meta i {
    color: var(--accent-color);
    margin-right: 5px;
}

/* Contact Button */
.contact-btn {
    display: block;
    width: 100%;
    padding: 12px;
    background: var(--accent-color);
    color: #fff;
    text-align: center;
    font-weight: bold;
    border: none;
    border-radius: 5px;
    margin-top: 15px;
    cursor: pointer;
    transition: background 0.3s;
}

.contact-btn:hover {
    background: #e69919;
}

/* Popup Form */
.popup {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: #fff;
    width: 90%;
    max-width: 400px;
    padding: 25px;
    border-radius: 10px;
    box-shadow: 0 8px 16px rgba(0,0,0,0.2);
    text-align: center;
    visibility: hidden;
    opacity: 0;
    transition: opacity 0.3s ease, visibility 0.3s ease;
}

.popup.active {
    visibility: visible;
    opacity: 1;
}

.popup-content {
    position: relative;
}

.close-popup {
    position: absolute;
    top: 10px;
    right: 10px;
    border: none;
    background: none;
    font-size: 1.5rem;
    cursor: pointer;
}

.popup h2 {
    margin-bottom: 20px;
}

.popup form input,
.popup form textarea {
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

.apply-btn {
    width: 100%;
    padding: 10px;
    background: var(--accent-color);
    border: none;
    color: white;
    font-weight: bold;
    border-radius: 5px;
    cursor: pointer;
    transition: background 0.3s;
}

.apply-btn:hover {
    background: #e69919;
}

/* Responsive Popup */
@media screen and (max-width: 480px) {
    .popup {
        width: 95%;
        padding: 20px;
    }
}

  </style>

</head>

<body>
    {{>whiteHeader}}
    <main>
<div class="container">
    <header class="page-header">
      <h1>Upcoming Seminars</h1>
      <p>Join our martial arts seminars and train with the best.</p>
    </header>

    <div id="seminars-grid" class="seminars-grid">
      <!-- Seminar cards will be injected here dynamically -->
    </div>
  </div>

  <!-- Popup Form for Seminar Application -->
  <div id="popup" class="popup hidden">
    <div class="popup-content">
      <button class="close-popup" onclick="closePopup()">&times;</button>
      <h2>Apply for Seminar</h2>
      <form id="applicationForm" >
        <input type="text" id="applicantName" name="name" placeholder="Your Name" minlength="8" autocomplete="on" required>
        <input type="email" id="applicantEmail" name="email" placeholder="Your Email" required  autocomplete="on" >
        <input type="tel" id="applicantPhone" name="phone" placeholder="Your Phone Number"   autocomplete="on" pattern="^\+?[0-9]{7,15}$" required>
        <textarea id="applicantMessage" name="message" autocomplete="on" placeholder="Why do you want to join?" rows="3" required minlength="50" maxlength="1000"></textarea>
        <button  class="apply-btn" type="submit">Apply</button>
      </form>
    </div>
  </div>

    </main>
    {{>Footer}}
  
  <script src="/js/pages/saminar.js">

  </script>
</body>
</html>
